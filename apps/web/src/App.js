// Web wrapper that re-uses the mobile App component
// Path: apps/web/src/App.js

import React from 'react';
// re-export the default export from the mobile App (TypeScript) file
import MobileApp from '@mobile/App';

export default MobileApp;
